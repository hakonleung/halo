# TimeRangeSelector 组件规格

## 概述
时间范围选择器，支持预设和自定义日期范围。

## Props

```typescript
interface TimeRangeSelectorProps {
  value: TimeRange;
  onChange: (range: TimeRange) => void;
  disabled?: boolean;
}

type TimeRange =
  | { type: 'preset'; value: 'today' | '7d' | '30d' | '90d' }
  | { type: 'custom'; start: string; end: string };
```

## 状态变体

| 状态 | 样式 |
|------|------|
| Default | 边框 `brand.matrix` 30%透明度 |
| Hover | 边框 `brand.matrix` 100% |
| Open | 下拉展开，边框发光 |
| Disabled | 灰色，不可点击 |

## 结构

```
┌─────────────────────────────┐
│  📅 最近7天           ▼    │
└─────────────────────────────┘
        ↓ (展开时)
┌─────────────────────────────┐
│  ○ 今日                     │
│  ● 最近7天                  │
│  ○ 最近30天                 │
│  ○ 最近90天                 │
│  ─────────────────────      │
│  ○ 自定义日期...            │
└─────────────────────────────┘
```

## 交互

1. 点击触发下拉
2. 选择预设立即生效
3. 选择"自定义"打开日期选择器
4. 点击外部关闭下拉
