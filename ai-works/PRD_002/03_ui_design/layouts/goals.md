# 页面布局：/goals

## 路由
`/goals`

## 关联用户故事
- US-002: 查看目标列表
- US-009: 目标筛选和排序

## 桌面端布局 (> 1024px)

```
┌─────────────────────────────────────────────────────────────────┐
│ TopNavbar (64px)                                                 │
├─────────────────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ Container (max-width: 1400px, padding: 24px)                │ │
│ │                                                             │ │
│ │ ┌─────────────────────────────────────────────────────────┐ │ │
│ │ │ HStack (justify: space-between, align: center)          │ │ │
│ │ │   Heading: "目标管理" (fontSize: 32px, color: text.neon)│ │ │
│ │ │   Button: "+ 创建目标" (Primary, brand.matrix)          │ │ │
│ │ └─────────────────────────────────────────────────────────┘ │ │
│ │                                                             │ │
│ │ ┌─────────────────────────────────────────────────────────┐ │ │
│ │ │ GoalFilters (bg.carbon, border, padding: 16px)          │ │ │
│ │ │   HStack (gap: 16px, wrap)                               │ │ │
│ │ │     Select: 状态筛选 (minW: 200px)                       │ │ │
│ │ │     Select: 分类筛选 (minW: 200px)                        │ │ │
│ │ │     Select: 排序 (minW: 180px)                           │ │ │
│ │ │     Input: 搜索框 (maxW: 300px)                          │ │ │
│ │ └─────────────────────────────────────────────────────────┘ │ │
│ │                                                             │ │
│ │ ┌─────────────────────────────────────────────────────────┐ │ │
│ │ │ SimpleGrid (columns: 3, gap: 16px)                      │ │ │
│ │ │   GoalCard (×N)                                         │ │ │
│ │ └─────────────────────────────────────────────────────────┘ │ │
│ └─────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

## 移动端布局 (< 640px)

```
┌─────────────────────────┐
│ TopNavbar (56px)        │
├─────────────────────────┤
│ Container (padding: 16px)│
│                         │
│ ┌─────────────────────┐ │
│ │ VStack (gap: 16px)  │ │
│ │   Heading: "目标管理"│ │
│ │   Button: "+ 创建目标"│ │
│ └─────────────────────┘ │
│                         │
│ ┌─────────────────────┐ │
│ │ GoalFilters         │ │
│ │   VStack (gap: 12px)│ │
│ │     Select: 状态    │ │
│ │     Select: 分类    │ │
│ │     Select: 排序    │ │
│ │     Input: 搜索      │ │
│ └─────────────────────┘ │
│                         │
│ ┌─────────────────────┐ │
│ │ VStack (gap: 12px)  │ │
│ │   GoalCard (×N)     │ │
│ └─────────────────────┘ │
└─────────────────────────┘
```

## 组件树

```
/goals Page
├── AuthenticatedLayout
│   └── TopNavbar
└── Container
    ├── PageHeader
    │   ├── Heading ("目标管理")
    │   └── Button ("+ 创建目标")
    ├── GoalFilters
    │   ├── Select (状态筛选)
    │   ├── Select (分类筛选)
    │   ├── Select (排序)
    │   └── Input (搜索)
    └── GoalList
        └── GoalCard (×N)
```

## 状态处理

### 加载状态
- 显示 Skeleton 组件替代 GoalCard

### 空状态
- 显示空状态提示："暂无目标，创建你的第一个目标"

### 错误状态
- 显示错误提示："加载失败，请重试"

